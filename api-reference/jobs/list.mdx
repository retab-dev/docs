---
title: "List Jobs"
openapi: "GET /v1/jobs"
---

<RequestExample>

```python python
from retab import Retab

client = Retab()

# List all jobs
jobs_response = client.jobs.list()
for job in jobs_response.data:
    print(f"{job.id}: {job.status} - {job.endpoint}")

# List with filters and pagination
jobs_response = client.jobs.list(
    status="completed",
    limit=10
)

# Paginate through results
if jobs_response.has_more:
    next_page = client.jobs.list(
        after=jobs_response.list_metadata.after,
        limit=10
    )
```

```javascript javascript
import { Retab } from "@retab/node";

const client = new Retab();

// List all jobs
const jobsResponse = await client.jobs.list();
for (const job of jobsResponse.data) {
  console.log(`${job.id}: ${job.status} - ${job.endpoint}`);
}

// List with filters and pagination
const filteredJobs = await client.jobs.list({
  status: "completed",
  limit: 10,
});

// Paginate through results
if (filteredJobs.list_metadata.after) {
  const nextPage = await client.jobs.list({
    after: filteredJobs.list_metadata.after,
    limit: 10,
  });
}
```

```go go
package main

import (
	"context"
	"fmt"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	// List all jobs
	jobsResponse, err := client.Jobs.List(ctx, nil)
	if err != nil {
		log.Fatal(err)
	}
	for _, job := range jobsResponse.Data {
		fmt.Printf("%s: %s - %s\n", job["id"], job["status"], job["endpoint"])
	}

	// List with filters and pagination
	filtered, err := client.Jobs.List(ctx, &retab.ListJobsParams{
		Status: "completed",
		Limit:  10,
	})
	if err != nil {
		log.Fatal(err)
	}

	// Paginate through results
	if filtered.HasMore {
		nextPage, err := client.Jobs.List(ctx, &retab.ListJobsParams{
			After: filtered.LastID,
			Limit: 10,
		})
		if err != nil {
			log.Fatal(err)
		}
		_ = nextPage
	}
}
```

```bash curl
# List all jobs
curl -X GET \
  'https://api.retab.com/v1/jobs' \
  -H "Api-Key: $RETAB_API_KEY"

# List with filters
curl -X GET \
  'https://api.retab.com/v1/jobs?status=completed&limit=10' \
  -H "Api-Key: $RETAB_API_KEY"

# Paginate
curl -X GET \
  'https://api.retab.com/v1/jobs?after=job_abc123&limit=10' \
  -H "Api-Key: $RETAB_API_KEY"
```

</RequestExample>

<ResponseExample>

```json 200 (include_request=true&include_response=true)
{
  "object": "list",
  "data": [
    {
      "id": "job_V1StGXR8_Z5jdHi6B-myT",
      "object": "job",
      "status": "completed",
      "endpoint": "/v1/extractions",
      "request": { ... },
      "response": {
        "status_code": 200,
        "body": { ... }
      },
      "error": null,
      "created_at": 1705420810,
      "started_at": 1705420812,
      "completed_at": 1705420820,
      "expires_at": 1706025610,
      "metadata": {"batch_id": "batch_001"}
    },
    {
      "id": "job_X2TuHYS9_A6keIj7C-nzU",
      "object": "job",
      "status": "queued",
      "endpoint": "/v1/parses",
      "request": { ... },
      "response": null,
      "error": null,
      "created_at": 1705420800,
      "started_at": null,
      "completed_at": null,
      "expires_at": 1706025600,
      "metadata": null
    }
  ],
  "first_id": "job_V1StGXR8_Z5jdHi6B-myT",
  "last_id": "job_X2TuHYS9_A6keIj7C-nzU",
  "has_more": true
}
```

```json 200 (empty)
{
  "object": "list",
  "data": [],
  "first_id": null,
  "last_id": null,
  "has_more": false
}
```

</ResponseExample>

## Pagination

Jobs are returned in descending order by creation time (newest first). Use id-based pagination to iterate through all results:

<CodeGroup>
```python Python
all_jobs = []
after = None

while True:
response = client.jobs.list(after=after, limit=100)
all_jobs.extend(response.data)

    if not response.has_more:
        break
    after = response.last_id

print(f"Total jobs: {len(all_jobs)}")

````

```javascript JavaScript
const allJobs = [];
let after = undefined;

while (true) {
    const response = await client.jobs.list({ after, limit: 100 });
    allJobs.push(...response.data);

    if (!response.has_more) {
        break;
    }
    after = response.last_id;
}

console.log(`Total jobs: ${allJobs.length}`);
````

```go Go
package main

import (
	"context"
	"fmt"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	allJobs := []retab.Job{}
	after := ""

	for {
		response, err := client.Jobs.List(ctx, &retab.ListJobsParams{
			After: after,
			Limit: 100,
		})
		if err != nil {
			log.Fatal(err)
		}
		allJobs = append(allJobs, response.Data...)

		if !response.HasMore {
			break
		}
		after = response.LastID
	}

	fmt.Printf("Total jobs: %d\n", len(allJobs))
}
```

</CodeGroup>

## Filtering Examples

### Monitor Active Jobs

<CodeGroup>
```python Python
# Get all jobs currently processing
in_progress = client.jobs.list(status="in_progress")
queued = client.jobs.list(status="queued")

print(f"In progress: {len(in_progress.data)}")
print(f"Queued: {len(queued.data)}")

````

```javascript JavaScript
// Get all jobs currently processing
const inProgress = await client.jobs.list({ status: "in_progress" });
const queued = await client.jobs.list({ status: "queued" });

console.log(`In progress: ${inProgress.data.length}`);
console.log(`Queued: ${queued.data.length}`);
````

```go Go
ctx := context.Background()
client, _ := retab.NewClient("")

// Get all jobs currently processing
inProgress, err := client.Jobs.List(ctx, &retab.ListJobsParams{Status: "in_progress"})
if err != nil {
	log.Fatal(err)
}
queued, err := client.Jobs.List(ctx, &retab.ListJobsParams{Status: "queued"})
if err != nil {
	log.Fatal(err)
}

fmt.Printf("In progress: %d\n", len(inProgress.Data))
fmt.Printf("Queued: %d\n", len(queued.Data))
```

</CodeGroup>

### Find Failed Jobs

<CodeGroup>
```python Python
# List failed jobs to investigate errors
failed_jobs = client.jobs.list(status="failed", limit=50)

for job in failed_jobs.data:
print(f"{job.id}: {job.error.code} - {job.error.message}")

````

```javascript JavaScript
// List failed jobs to investigate errors
const failedJobs = await client.jobs.list({ status: "failed", limit: 50 });

for (const job of failedJobs.data) {
    console.log(`${job.id}: ${job.error.code} - ${job.error.message}`);
}
````

```go Go
ctx := context.Background()
client, _ := retab.NewClient("")

// List failed jobs to investigate errors
failedJobs, err := client.Jobs.List(ctx, &retab.ListJobsParams{
	Status: "failed",
	Limit:  50,
})
if err != nil {
	log.Fatal(err)
}

for _, job := range failedJobs.Data {
	if jobErr, ok := job["error"].(map[string]any); ok {
		fmt.Printf("%s: %s - %s\n", job["id"], jobErr["code"], jobErr["message"])
	}
}
```

</CodeGroup>

### Filter by Metadata

While the API doesn't support direct metadata filtering, you can filter client-side:

<CodeGroup>
```python Python
# Find jobs from a specific batch
batch_id = "batch_001"
batch_jobs = []

response = client.jobs.list(limit=100)
for job in response.data:
if job.metadata and job.metadata.get("batch_id") == batch_id:
batch_jobs.append(job)

print(f"Jobs in {batch_id}: {len(batch_jobs)}")

````

```javascript JavaScript
// Find jobs from a specific batch
const batchId = "batch_001";
const batchJobs = [];

const response = await client.jobs.list({ limit: 100 });
for (const job of response.data) {
    if (job.metadata?.batch_id === batchId) {
        batchJobs.push(job);
    }
}

console.log(`Jobs in ${batchId}: ${batchJobs.length}`);
````

```go Go
ctx := context.Background()
client, _ := retab.NewClient("")

// Find jobs from a specific batch
batchID := "batch_001"
batchJobs := []retab.Job{}

response, err := client.Jobs.List(ctx, &retab.ListJobsParams{Limit: 100})
if err != nil {
	log.Fatal(err)
}
for _, job := range response.Data {
	metadata, ok := job["metadata"].(map[string]any)
	if !ok {
		continue
	}
	if metadata["batch_id"] == batchID {
		batchJobs = append(batchJobs, job)
	}
}

fmt.Printf("Jobs in %s: %d\n", batchID, len(batchJobs))
```

</CodeGroup>
