---
title: Retry job
openapi: "POST /v1/jobs/{job_id}/retry"
---

Retry a failed legacy job.

<Note>
  The generic Jobs API is documented for existing integrations. New SDK clients should use background mode on primitive create endpoints instead.
</Note>

```python Python
import os
import urllib.request

request = urllib.request.Request(
    "https://api.retab.com/v1/jobs/job_123/retry",
    data='{}'.encode(), method="POST",
    headers={"Authorization": f"Bearer {os.environ['RETAB_API_KEY']}"},
)
with urllib.request.urlopen(request) as response:
    print(response.status)
```

```typescript TypeScript
const response = await fetch("https://api.retab.com/v1/jobs/job_123/retry", {
  method: "POST",
  headers: { Authorization: `Bearer ${process.env.RETAB_API_KEY}` },
  body: JSON.stringify({}),
});

console.log(response.status);
```

```go Go
package main

import (
    "bytes"
    "fmt"
    "net/http"
    "os"
)

func main() {
    request, err := http.NewRequest("POST", "https://api.retab.com/v1/jobs/job_123/retry", bytes.NewReader([]byte(`{}`)))
    if err != nil {
        panic(err)
    }
    request.Header.Set("Authorization", "Bearer "+os.Getenv("RETAB_API_KEY"))
    response, err := http.DefaultClient.Do(request)
    if err != nil {
        panic(err)
    }
    defer response.Body.Close()
    fmt.Println(response.StatusCode)
}
```

```java Java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

public final class Example {
  public static void main(String[] args) throws Exception {
    HttpRequest request = HttpRequest.newBuilder()
        .uri(URI.create("https://api.retab.com/v1/jobs/job_123/retry"))
        .header("Authorization", "Bearer " + System.getenv("RETAB_API_KEY"))
        .method("POST", java.net.http.HttpRequest.BodyPublishers.ofString("{}"))
        .build();

    HttpResponse<String> response = HttpClient.newHttpClient()
        .send(request, HttpResponse.BodyHandlers.ofString());
    System.out.println(response.statusCode());
  }
}
```

```bash curl
curl -X POST 'https://api.retab.com/v1/jobs/job_123/retry' \
  -H "Authorization: Bearer $RETAB_API_KEY" \
  --data '{}'
```
