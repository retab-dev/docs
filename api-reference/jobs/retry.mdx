---
title: "Retry Job"
openapi: "POST /v1/jobs/{job_id}/retry"
---

<RequestExample>

```python Python
from retab import Retab

client = Retab()

retried_job = client.jobs.retry("job_V1StGXR8_Z5jdHi6B-myT")

print(retried_job.status)  # queued
```

```typescript TypeScript
import { Retab } from "@retab/node";

const client = new Retab({ apiKey: process.env.RETAB_API_KEY });

const retriedJob = await client.jobs.retry("job_V1StGXR8_Z5jdHi6B-myT");

console.log(retriedJob.status); // queued
```

```go Go
package main

import (
	"context"
	"fmt"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	retriedJob, err := client.Jobs.Retry(ctx, "job_V1StGXR8_Z5jdHi6B-myT")
	if err != nil {
		log.Fatal(err)
	}

	fmt.Println((*retriedJob)["status"]) // queued
}
```

```ruby Ruby
require 'retab'

client = Retab::Client.new(api_key: ENV['RETAB_API_KEY'])

retried_job = client.jobs.retry(job_id: 'job_V1StGXR8_Z5jdHi6B-myT')

puts retried_job.status # queued
```

```rust Rust
use retab::Retab;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Retab::new(std::env::var("RETAB_API_KEY")?);

    let retried_job = client.jobs().retry("job_V1StGXR8_Z5jdHi6B-myT").await?;

    println!("{:?}", retried_job.status); // queued
    Ok(())
}
```

```php PHP
<?php
require 'vendor/autoload.php';

use Retab\Client;

$client = new Client(apiKey: getenv('RETAB_API_KEY'));

$result = $client->jobs()->retry(
    jobId: 'job_abc123',
);
print_r($result);
```

```csharp C#
using Retab;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;
var client = new Retab(apiKey);

var result = await client.Jobs.RetryAsync("job_abc123", apiKey);
Console.WriteLine(result);
```

```curl cURL
curl -X POST \
  'https://api.retab.com/v1/jobs/job_V1StGXR8_Z5jdHi6B-myT/retry' \
  -H "Api-Key: $RETAB_API_KEY"
```

</RequestExample>

<ResponseExample>

```json 200
{
  "id": "job_V1StGXR8_Z5jdHi6B-myT",
  "object": "job",
  "status": "queued",
  "endpoint": "/v1/extractions",
  "request": {
    "document": {
      "filename": "invoice.pdf",
      "url": "data:application/pdf;base64,..."
    },
    "json_schema": { ... },
    "model": "retab-small"
  },
  "response": null,
  "error": null,
  "created_at": 1705420800,
  "started_at": null,
  "completed_at": null,
  "expires_at": 1706025600,
  "metadata": {"batch_id": "batch_001"}
}
```

```json 400
{
  "detail": "Cannot retry job in 'completed' status"
}
```

```json 404
{
  "detail": "Job not found"
}
```

```json 503
{
  "detail": "Retry queued; dispatch is delayed"
}
```

</ResponseExample>

## Retry Behavior

Retry resets the existing job in place and keeps the same `job_id`.

Retry is supported when the current status is:

- `failed`
- `cancelled`
- `expired` if artifact cleanup has not already completed

On success, Retab clears the previous `error`, removes the previous `response`, refreshes `expires_at`, and requeues the job for execution.

If task dispatch is temporarily delayed after the retry is accepted, the API returns `503` with `"Retry queued; dispatch is delayed"`. The retry still succeeded, so poll the same `job_id`.
