---
title: "Apply Workflow Spec"
openapi: "POST /v1/workflows/spec/apply"
---

Apply a declarative workflow YAML spec to draft workflow state.

`apply()` writes the canonical draft graph represented by the YAML spec. It does not publish. Call the workflow publish method separately when the draft is ready to become the live version.

<RequestExample>

```python python
from retab import Retab

client = Retab()

result = client.workflows.spec.apply(yaml_definition)

print(result.workflow_id)
print(result.created)
print(result.summary.has_changes)
```

```typescript TypeScript
import { Retab } from "@retab/node";

const client = new Retab({ apiKey: process.env.RETAB_API_KEY });

const result = await client.workflows.specs.apply(yamlDefinition);

console.log(result.workflowId);
console.log(result.created);
console.log(result.summary.hasChanges);
```

```go go
package main

import (
	"context"
	"fmt"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	yamlDefinition := "<workflow-yaml>"
	result, err := client.Workflows.Spec.Apply(ctx, yamlDefinition)
	if err != nil {
		log.Fatal(err)
	}

	fmt.Println((*result)["workflow_id"])
	fmt.Println((*result)["created"])
	fmt.Println((*result)["summary"])
}
```

```ruby Ruby
require 'retab'

client = Retab::Client.new(api_key: ENV['RETAB_API_KEY'])

result = client.workflow_specs.apply(yaml_definition: yaml_definition)

puts result.workflow_id
puts result.created
puts result.summary.has_changes
```

```rust rust
use retab::models::DeclarativeWorkflowRequest;
use retab::resources::workflow_specs::ApplyParams;
use retab::Retab;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Retab::new(std::env::var("RETAB_API_KEY")?);

    let yaml_definition = "<workflow-yaml>";
    let result = client
        .workflow_specs()
        .apply(ApplyParams::new(DeclarativeWorkflowRequest::new(
            yaml_definition,
        )))
        .await?;

    println!("{}", result.workflow_id);
    println!("{}", result.created);
    if let Some(summary) = &result.summary {
        println!("{:?}", summary.has_changes);
    }
    Ok(())
}
```

```php PHP
<?php
require 'vendor/autoload.php';

use Retab\Client;

$client = new Client(apiKey: getenv('RETAB_API_KEY'));

$result = $client->workflowSpecs()->apply(
    yamlDefinition: 'value',
);
print_r($result);
```

```csharp C#
using Retab;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;
var client = new Retab(apiKey);

var result = await client.WorkflowSpecs.ApplyAsync(apiKey, new WorkflowSpecsApplyOptions());
Console.WriteLine(result);
```

```bash curl
curl -X 'POST' \
  'https://api.retab.com/v1/workflows/spec/apply' \
  -H 'Content-Type: application/json' \
  -H 'Api-Key: <your-api-key>' \
  -d '{
    "yaml_definition": "<workflow-yaml>"
  }'
```

</RequestExample>

<ResponseExample>

```json 200
{
  "workflow_id": "wf_abc123",
  "created": false,
  "block_count": 2,
  "edge_count": 1,
  "diagnostics": {
    "issues": []
  },
  "format_version": "workflows-plan/v1",
  "summary": {
    "add": 0,
    "change": 0,
    "destroy": 0,
    "replace": 0,
    "noop": 1,
    "total": 0,
    "has_changes": false
  },
  "resource_changes": [],
  "rendered_plan": "No changes. Infrastructure is up-to-date."
}
```

</ResponseExample>
