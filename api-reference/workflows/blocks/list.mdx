---
title: "List Blocks"
openapi: "GET /v1/workflows/blocks"
---

List every authored block in a workflow's current draft.

Returns the canonical `{ "data": [...], "list_metadata": { "before": null, "after": null } }` pagination envelope shared with all Retab list endpoints. Cursor pagination is not yet implemented for this endpoint — `list_metadata` is always `{ before: null, after: null }`.

Use this endpoint with [List Edges](/api-reference/workflows/edges/list) when you need to reconstruct a workflow graph.

<RequestExample>

```python Python
from retab import Retab

client = Retab()

blocks = client.workflows.blocks.list("wf_abc123xyz")

for block in blocks.data:
    print(f"{block.id} ({block.type}): {block.label}")
```

```typescript TypeScript
import { Retab } from "@retab/node";

const client = new Retab({ apiKey: process.env.RETAB_API_KEY });

const blocks = await client.workflows.blocks.list({ workflowId: "wf_abc123xyz" });

for (const block of blocks.data) {
  console.log(`${block.id} (${block.type}): ${block.label}`);
}
```

```go Go
package main

import (
	"context"
	"fmt"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	blocks, err := client.Workflows.Blocks.List(ctx, "wf_abc123xyz", nil)
	if err != nil {
		log.Fatal(err)
	}

	for _, block := range blocks.Data {
		fmt.Printf("%s (%s): %s\n", block.ID, block.Type, block.Label)
	}
}
```

```ruby Ruby
require 'retab'

client = Retab::Client.new(api_key: ENV['RETAB_API_KEY'])

blocks = client.workflow_blocks.list(workflow_id: 'wf_abc123xyz')

blocks.data.each do |block|
  puts "#{block.id} (#{block.type}): #{block.label}"
end
```

```rust Rust
use retab::resources::workflow_blocks::ListParams;
use retab::Retab;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Retab::new(std::env::var("RETAB_API_KEY")?);

    let blocks = client
        .workflow_blocks()
        .list(ListParams::new("wf_abc123xyz"))
        .await?;

    for block in &blocks.data {
        println!(
            "{} ({}): {}",
            block.id,
            block.type_,
            block.label.as_deref().unwrap_or("")
        );
    }
    Ok(())
}
```

```php PHP
<?php
require 'vendor/autoload.php';

use Retab\Client;

$client = new Client(apiKey: getenv('RETAB_API_KEY'));

$result = $client->workflowBlocks()->list(
    workflowId: 'wf_abc123',
);
print_r($result);
```

```csharp C#
using Retab;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;
var client = new Retab(apiKey);

var result = await client.WorkflowBlocks.ListAsync(apiKey, new WorkflowBlocksListOptions());
Console.WriteLine(result);
```

```curl cURL
curl -X 'GET' \
  'https://api.retab.com/v1/workflows/blocks?workflow_id=wf_abc123xyz' \
  -H 'Api-Key: <your-api-key>'
```

</RequestExample>

<ResponseExample>

```json 200
{
  "data": [
    {
      "id": "start-1",
      "workflow_id": "wf_abc123xyz",
      "type": "start_document",
      "label": "Invoice Input",
      "position_x": 0,
      "position_y": 0,
      "width": 240,
      "height": 120,
      "config": null,
      "parent_id": null,
      "updated_at": "2026-04-30T17:00:00Z"
    },
    {
      "id": "extract-1",
      "workflow_id": "wf_abc123xyz",
      "type": "extract",
      "label": "Extract Invoice Fields",
      "position_x": 320,
      "position_y": 0,
      "width": 240,
      "height": 120,
      "config": {
        "model": "gpt-5",
        "json_schema": {
          "type": "object",
          "properties": {
            "invoice_number": { "type": "string" },
            "total": { "type": "number" }
          }
        }
      },
      "parent_id": null,
      "updated_at": "2026-05-01T14:30:00Z"
    }
  ],
  "list_metadata": {
    "before": null,
    "after": null
  }
}
```

```json 404
{
  "detail": "Workflow not found"
}
```

</ResponseExample>
