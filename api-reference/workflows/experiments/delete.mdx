---
title: "Delete Experiment"
openapi: "DELETE /v1/workflows/experiments/{experiment_id}"
---

Permanently delete an experiment along with all its runs and per-document
results.

Returns `409` if any run is still pending or running. Cancel the in-flight run
with [Cancel Experiment Run](/api-reference/workflows/experiments/runs/cancel)
first.

<RequestExample>

```python Python
from retab import Retab

client = Retab()

client.workflows.experiments.delete(
    experiment_id="exp_abc",
)
```

```typescript TypeScript
import { Retab } from "@retab/node";

const client = new Retab({ apiKey: process.env.RETAB_API_KEY });

await client.workflows.experiments.delete("exp_abc");
```

```go Go
package main

import (
	"context"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func ptr[T any](v T) *T { return &v }

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	if err := client.Workflows.Experiments.Delete(ctx, "exp_abc"); err != nil {
		log.Fatal(err)
	}
}
```

```ruby Ruby
require 'retab'

client = Retab::Client.new(api_key: ENV['RETAB_API_KEY'])

client.workflow_experiments.delete(experiment_id: 'exp_abc')
```

```rust Rust
use retab::Retab;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Retab::new(std::env::var("RETAB_API_KEY")?);

    client.workflows().experiments().delete("exp_abc").await?;
    Ok(())
}
```

```php PHP
<?php
require 'vendor/autoload.php';

use Retab\Client;

$client = new Client(apiKey: getenv('RETAB_API_KEY'));

$client->workflowExperiments()->delete(
    experimentId: 'exp_abc123',
);
echo "Deleted\n";
```

```csharp C#
using Retab;
using RetabClient = Retab.Retab;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;
var client = new RetabClient(apiKey);

await client.Workflows.Experiments.DeleteAsync("exp_abc123");
Console.WriteLine("Deleted");
```

```java Java
import com.retab.RetabClient;

public final class Example {
  public static void main(String[] args) throws Exception {
    RetabClient client = new RetabClient(System.getenv("RETAB_API_KEY"));

    var result = client.workflows().experiments().delete("exp_abc123");
    System.out.println(result);
  }
}
```

```curl cURL
curl -X 'DELETE' \
  'https://api.retab.com/v1/workflows/experiments/exp_abc' \
  -H 'Authorization: Bearer <your-api-key>'
```

</RequestExample>

<ResponseExample>

```json 204

```

```json 409
{
  "detail": "Cannot delete a running experiment. Cancel it first."
}
```

</ResponseExample>
