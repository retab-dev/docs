---
title: "Get Experiment Run Metrics"
openapi: "GET /v1/workflows/experiments/metrics"
---

Get metrics for a specific experiment run. Metrics are consensus likelihoods on
the `[0.0, 1.0]` scale where `0.0` is low agreement and `1.0` is total
agreement.

The `view` query parameter selects one of four shapes:

| View          | Use it to                                                                                                     |
| ------------- | ------------------------------------------------------------------------------------------------------------- |
| `summary`     | Read the overall score plus block-specific aggregates. Start here.                                            |
| `by_document` | Drill into one document and see all its targets, sorted ascending. Requires `document_id`.                    |
| `by_target`   | Drill into one target and see its score across every document. Requires `target_path`.                        |
| `votes`       | See the per-voter consensus rows for one document/target cell. Requires both `document_id` and `target_path`. |

Pass `include_prior=false` to omit prior-run comparison fields, or
`prior_run_id=...` to override which run is treated as the prior.

<RequestExample>

```python python
from retab import Retab

client = Retab()

summary = client.workflows.experiments.runs.metrics.get(
    "exprun_2",
    view="summary",
)

target_view = client.workflows.experiments.runs.metrics.get(
    "exprun_2",
    view="by_target",
    target_path="line_items.*.unit_price",
)
```

```javascript javascript
import { Retab } from "@retab/node";

const client = new Retab();

const summary = await client.workflows.experiments.runs.metrics.get({
  runId: "exprun_2",
  view: "summary",
});

const targetView = await client.workflows.experiments.runs.metrics.get({
  runId: "exprun_2",
  view: "by_target",
  targetPath: "line_items.*.unit_price",
});
```

```go go
package main

import (
	"context"
	"fmt"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	summary, err := client.Workflows.Experiments.Runs.Metrics.Get(
		ctx,
		"exprun_2",
		&retab.GetExperimentMetricsParams{View: "summary"},
	)
	if err != nil {
		log.Fatal(err)
	}

	targetView, err := client.Workflows.Experiments.Runs.Metrics.Get(
		ctx,
		"exprun_2",
		&retab.GetExperimentMetricsParams{
			View:       "by_target",
			TargetPath: "line_items.*.unit_price",
		},
	)
	if err != nil {
		log.Fatal(err)
	}

	fmt.Println(summary["score"], targetView["view"])
}
```

```bash curl
curl -X 'GET' \
  'https://api.retab.com/v1/workflows/experiments/metrics?run_id=exprun_2&view=summary' \
  -H 'Api-Key: <your-api-key>'

curl -X 'GET' \
  'https://api.retab.com/v1/workflows/experiments/metrics?run_id=exprun_2&view=by_target&target_path=total' \
  -H 'Api-Key: <your-api-key>'
```

</RequestExample>

<ResponseExample>

```json 200
{
  "experiment_id": "exp_abc",
  "run_id": "exprun_2",
  "view": "summary",
  "definition_fingerprint": "deadbeef",
  "block_kind": "extract",
  "score": 0.83,
  "prior_score": 0.79,
  "prior_run_id": "exprun_1",
  "documents": [
    {
      "id": "expdoc_1",
      "filename": "a.pdf",
      "score": 0.91,
      "prior_score": 0.85
    }
  ],
  "aggregate": {
    "likelihoods": { "total": 0.92, "vendor.name": 0.78 }
  }
}
```

</ResponseExample>
