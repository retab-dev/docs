---
title: "Cancel Workflow Test Run"
openapi: "POST /v1/workflows/tests/runs/{run_id}/cancel"
---

Cancel a pending or running workflow-test run by `run_id`. Cancellation is
run-specific; you do not pass the workflow id or test id when cancelling.

<RequestExample>

```python python
from retab import Retab

client = Retab()

run = client.workflows.tests.runs.cancel("wftestrun_q1z2")
print(run.lifecycle.status)
```

```javascript javascript
import { Retab } from "@retab/node";

const client = new Retab();

const run = await client.workflows.tests.runs.cancel({
  runId: "wftestrun_q1z2",
});
console.log(run.lifecycle.status);
```

```go go
package main

import (
	"context"
	"fmt"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	run, err := client.Workflows.Tests.Runs.Cancel(ctx, "wftestrun_q1z2")
	if err != nil {
		log.Fatal(err)
	}

	fmt.Println(run.Lifecycle.Status)
}
```

```bash curl
curl -X 'POST' \
  'https://api.retab.com/v1/workflows/tests/runs/wftestrun_q1z2/cancel' \
  -H 'Api-Key: <your-api-key>'
```

</RequestExample>

<ResponseExample>

```json 200
{
  "id": "wftestrun_q1z2",
  "lifecycle": { "status": "cancelled" },
  "timing": {
    "created_at": "2026-05-18T10:00:00Z",
    "started_at": "2026-05-18T10:00:01Z",
    "completed_at": "2026-05-18T10:00:04Z"
  }
}
```

</ResponseExample>
