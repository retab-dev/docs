---
title: "List Test Results"
openapi: "GET /v1/workflows/tests/results"
---

List the test results produced by one workflow-test run. Filter by `run_id`;
each result also has
its own opaque `id`; use [Get Test Run Result](/api-reference/workflows/tests/results/get)
to refresh one row directly.

The response uses the canonical Retab list envelope:
`{ "data": [...], "list_metadata": { "before": null, "after": null } }`.

<RequestExample>

```python Python
from retab import Retab

client = Retab()

results = client.workflows.tests.results.list("wftestrun_q1z2")
for result in results.data:
    print(result.test_id, result.verdict)
```

```typescript TypeScript
import { Retab } from "@retab/node";

const client = new Retab({ apiKey: process.env.RETAB_API_KEY });

const results = await client.workflows.tests.results.list({
  runId: "wftestrun_q1z2",
});
for (const result of results.data) {
  console.log(result.testId, result.verdict);
}
```

```go Go
package main

import (
	"context"
	"fmt"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func ptr[T any](v T) *T { return &v }

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	results, err := client.Workflows.Tests.Results.List(ctx, &retab.WorkflowTestRunResultsListParams{
		RunID: "wftestrun_q1z2",
	})
	if err != nil {
		log.Fatal(err)
	}

	for _, result := range results.Data {
		fmt.Println(result.TestID, result.Verdict)
	}
}
```

```ruby Ruby
require 'retab'

client = Retab::Client.new(api_key: ENV['RETAB_API_KEY'])

results = client.workflow_test_run_results.list(run_id: 'wftestrun_q1z2')
results.data.each do |result|
  puts "#{result.test_id} #{result.verdict}"
end
```

```rust Rust
use retab::resources::workflow_test_run_results::ListParams;
use retab::Retab;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Retab::new(std::env::var("RETAB_API_KEY")?);

    let results = client
        .workflows().tests().results()
        .list(ListParams::new("wftestrun_q1z2"))
        .await?;
    for result in &results.data {
        println!("{} {:?}", result.test_id, result.verdict);
    }
    Ok(())
}
```

```php PHP
<?php
require 'vendor/autoload.php';

use Retab\Client;

$client = new Client(apiKey: getenv('RETAB_API_KEY'));

$result = $client->workflowTestRunResults()->list(
    runId: 'run_abc123',
);
print_r($result);
```

```csharp C#
using Retab;
using RetabClient = Retab.Retab;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;
var client = new RetabClient(apiKey);

var result = await client.Workflows.Tests.Results.ListAsync(new WorkflowTestRunResultsListOptions());
Console.WriteLine(result);
```

```java Java
import com.retab.RetabClient;

public final class Example {
  public static void main(String[] args) throws Exception {
    RetabClient client = new RetabClient(System.getenv("RETAB_API_KEY"));

    var result = client.workflows().tests().results().list("run_abc123", null, null, 10L, null);
    System.out.println(result);
  }
}
```

```curl cURL
curl -X 'GET' \
  'https://api.retab.com/v1/workflows/tests/results?run_id=wftestrun_q1z2' \
  -H 'Authorization: Bearer <your-api-key>'
```

</RequestExample>

<ResponseExample>

```json 200
{
  "data": [
    {
      "id": "wfnodetestrun_a",
      "run_id": "wftestrun_q1z2",
      "test_id": "wfnodetest_a",
      "target": { "type": "block", "block_id": "block_extract_invoice" },
      "lifecycle": { "status": "completed" },
      "timing": {
        "started_at": "2026-05-18T10:00:02Z",
        "completed_at": "2026-05-18T10:00:20Z",
        "duration_ms": 18221
      },
      "outputs": {
        "output-json-0": { "total": 1234.56, "vendor": { "name": "Acme Inc" } }
      },
      "assertion_result": {
        "assertion_id": "assert_xyz",
        "condition_kind": "equals",
        "status": "passed",
        "actual_value": 1234.56,
        "expected_value": 1234.56,
        "failure": null
      },
      "verdict": { "status": "passed" }
    }
  ],
  "list_metadata": {
    "before": null,
    "after": null
  }
}
```

</ResponseExample>
