---
title: 'Delete Edge'
openapi: 'DELETE /v1/workflows/{workflow_id}/edges/{edge_id}'
---

Delete a single edge.

To remove an entire block and all its incident edges in one call, use [Delete Block](/api-reference/workflows/blocks/delete) — it cleans up edges automatically. To wipe every edge in a workflow, see [Delete All Edges](/api-reference/workflows/edges/delete-all).

<RequestExample>

```python python
from retab import Retab

client = Retab()

client.workflows.edges.delete(
    workflow_id="wf_abc123xyz",
    edge_id="edge-extract-to-review",
)
```

```javascript javascript
import { Retab } from '@retab/node';

const client = new Retab();

await client.workflows.edges.delete("wf_abc123xyz", "edge-extract-to-review");
```

```go go
package main

import (
	"context"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	if err := client.Workflows.Edges.Delete(ctx, "wf_abc123xyz", "edge-extract-to-review"); err != nil {
		log.Fatal(err)
	}
}
```

```bash curl
curl -X 'DELETE' \
  'https://api.retab.com/v1/workflows/wf_abc123xyz/edges/edge-extract-to-review' \
  -H 'Api-Key: <your-api-key>'
```

</RequestExample>

<ResponseExample>

```text 204
(empty body)
```

```json 404
{
  "detail": "Edge not found"
}
```

</ResponseExample>
