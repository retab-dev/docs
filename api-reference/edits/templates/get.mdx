---
title: 'Get Template'
openapi: 'GET /v1/edits/templates/{template_id}'
---

<RequestExample>

```python python
from retab import Retab

client = Retab()
template = client.edits.templates.get("edittplt_abc123")
print(template.name, template.field_count)
```

```javascript javascript
import { Retab } from '@retab/node';

const client = new Retab();
const template = await client.edits.templates.get("edittplt_abc123");
console.log(template.name, template.field_count);
```

```bash curl
curl -X GET \
  'https://api.retab.com/v1/edits/templates/edittplt_abc123' \
  -H "Api-Key: $RETAB_API_KEY"
```

</RequestExample>

<ResponseExample>

```json 200
{
  "id": "edittplt_abc123",
  "name": "W-9 Form",
  "file": {
    "id": "file_6dd6eb00688ad8d1",
    "filename": "w9_empty.pdf",
    "mime_type": "application/pdf"
  },
  "form_fields": [
    {"key": "full_name", "description": "Full legal name", "type": "text", "bbox": {"left": 0.15, "top": 0.20, "width": 0.35, "height": 0.03, "page": 1}}
  ],
  "field_count": 1,
  "organization_id": "org_abc123",
  "created_at": "2024-03-15T10:30:00Z",
  "updated_at": "2024-03-15T10:30:00Z"
}
```

```json 404
{
  "detail": "Template edittplt_abc123 not found"
}
```

</ResponseExample>

## Path Parameters

<ParamField path="template_id" type="string" required>
  The unique identifier of the template to retrieve.
</ParamField>
