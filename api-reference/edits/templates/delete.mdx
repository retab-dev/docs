---
title: "Delete Template"
openapi: "DELETE /v1/edits/templates/{template_id}"
---

Delete an `EditTemplate` by ID. Existing `Edit` records that referenced this template are preserved but new calls to `POST /v1/edits` with this template id will return `404`.

<RequestExample>

```python Python
from retab import Retab

client = Retab()
client.edits.templates.delete("edittplt_abc123")
```

```typescript TypeScript
import { Retab } from "@retab/node";

const client = new Retab({ apiKey: process.env.RETAB_API_KEY });
await client.edits.templates.delete("edittplt_abc123");
```

```go Go
package main

import (
	"context"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func ptr[T any](v T) *T { return &v }

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	if err := client.Edits.Templates.Delete(ctx, "edittplt_abc123"); err != nil {
		log.Fatal(err)
	}
}
```

```ruby Ruby
require 'retab'

client = Retab::Client.new(api_key: ENV['RETAB_API_KEY'])

client.edit_templates.delete(template_id: 'edittplt_abc123')
```

```rust Rust
use retab::Retab;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Retab::new(std::env::var("RETAB_API_KEY")?);
    client.edits().templates().delete("edittplt_abc123").await?;
    Ok(())
}
```

```php PHP
<?php
require 'vendor/autoload.php';

use Retab\Client;

$client = new Client(apiKey: getenv('RETAB_API_KEY'));

$client->editTemplates()->delete(
    templateId: 'tmpl_abc123',
);
echo "Deleted\n";
```

```csharp C#
using Retab;
using RetabClient = Retab.Retab;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;
var client = new RetabClient(apiKey);

await client.Edits.Templates.DeleteAsync("tmpl_abc123");
Console.WriteLine("Deleted");
```

```java Java
import com.retab.RetabClient;

public final class Example {
  public static void main(String[] args) throws Exception {
    RetabClient client = new RetabClient(System.getenv("RETAB_API_KEY"));

    var result = client.edits().templates().delete("tmpl_abc123");
    System.out.println(result);
  }
}
```

```curl cURL
curl -X DELETE \
  'https://api.retab.com/v1/edits/templates/edittplt_abc123' \
  -H "Authorization: Bearer $RETAB_API_KEY"
```

</RequestExample>

<ResponseExample>

```text 204
No Content
```

```json 404
{
  "detail": "Template edittplt_abc123 not found"
}
```

</ResponseExample>
