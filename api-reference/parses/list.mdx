---
title: "List Parses"
openapi: "GET /v1/parses"
---

List persisted parses for your organization, with id-based pagination and optional filename / date filtering.

<RequestExample>

```python python
from datetime import datetime
from retab import Retab

client = Retab()

# List recent parses
parses = client.parses.list(limit=20, order="desc")
for parse in parses.data:
    print(f"{parse.id}: {parse.file.filename}")

# Filter by date range
parses = client.parses.list(
    from_date=datetime(2024, 1, 1),
    to_date=datetime(2024, 12, 31),
    limit=50,
)
```

```javascript javascript
import { Retab } from "@retab/node";

const client = new Retab();

// List recent parses
const parses = await client.parses.list({ limit: 20, order: "desc" });
for (const parse of parses.data) {
  console.log(`${parse.id}: ${parse.file.filename}`);
}

// Filter by date range
const ranged = await client.parses.list({
  from_date: new Date("2024-01-01"),
  to_date: new Date("2024-12-31"),
  limit: 50,
});
```

```go go
package main

import (
	"context"
	"fmt"
	"log"
	"time"

	retab "github.com/retab-dev/retab/clients/go"
)

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	// List recent parses
	parses, err := client.Parses.List(ctx, &retab.ListParams{Limit: 20, Order: "desc"})
	if err != nil {
		log.Fatal(err)
	}
	for _, parse := range parses.Data {
		fmt.Printf("%v: %v\n", parse["id"], parse["file"])
	}

	// Filter by date range
	from := time.Date(2024, 1, 1, 0, 0, 0, 0, time.UTC)
	to := time.Date(2024, 12, 31, 0, 0, 0, 0, time.UTC)
	ranged, err := client.Parses.List(ctx, &retab.ListParams{
		FromDate: &from,
		ToDate:   &to,
		Limit:    50,
	})
	if err != nil {
		log.Fatal(err)
	}
	fmt.Println(ranged)
}
```

```bash curl
# List recent parses
curl -X GET \
  'https://api.retab.com/v1/parses?limit=20&order=desc' \
  -H "Api-Key: $RETAB_API_KEY"

# Filter by filename prefix and date range
curl -X GET \
  'https://api.retab.com/v1/parses?filename=invoice&from_date=2024-01-01&to_date=2024-12-31&limit=50' \
  -H "Api-Key: $RETAB_API_KEY"
```

</RequestExample>

<ResponseExample>

```json 200
{
  "data": [
    {
      "id": "parse_01G34H8J2K",
      "file": {
        "id": "file_6dd6eb00688ad8d1",
        "filename": "invoice.pdf",
        "mime_type": "application/pdf"
      },
      "model": "retab-small",
      "table_parsing_format": "markdown",
      "image_resolution_dpi": 192,
      "output": {
        "pages": ["Page 1 text...", "Page 2 text..."],
        "text": "Page 1 text...\n\nPage 2 text..."
      },
      "usage": {
        "page_count": 2,
        "credits": 1.0
      },
      "created_at": "2024-03-15T10:30:00Z"
    }
  ],
  "list_metadata": {
    "before": null,
    "after": "parse_01G34H8J2K",
    "total_count": 42
  }
}
```

</ResponseExample>
