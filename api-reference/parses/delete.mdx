---
title: "Delete Parse"
openapi: "DELETE /v1/parses/{parse_id}"
---

Permanently delete a persisted parse. Returns `204 No Content` on success.

<RequestExample>

```python Python
from retab import Retab

client = Retab()

client.parses.delete("parse_01G34H8J2K")
```

```typescript TypeScript
import { Retab } from "@retab/node";

const client = new Retab({ apiKey: process.env.RETAB_API_KEY });

await client.parses.delete("parse_01G34H8J2K");
```

```go Go
package main

import (
	"context"
	"log"

	retab "github.com/retab-dev/retab/clients/go"
)

func ptr[T any](v T) *T { return &v }

func main() {
	ctx := context.Background()

	client, err := retab.NewClient("")
	if err != nil {
		log.Fatal(err)
	}

	if err := client.Parses.Delete(ctx, "parse_01G34H8J2K"); err != nil {
		log.Fatal(err)
	}
}
```

```ruby Ruby
require 'retab'

client = Retab::Client.new(api_key: ENV['RETAB_API_KEY'])

client.parses.delete(parse_id: 'parse_01G34H8J2K')
```

```rust Rust
use retab::Retab;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let client = Retab::new(std::env::var("RETAB_API_KEY")?);

    client.parses().delete("parse_01G34H8J2K").await?;
    Ok(())
}
```

```php PHP
<?php
require 'vendor/autoload.php';

use Retab\Client;

$client = new Client(apiKey: getenv('RETAB_API_KEY'));

$client->parses()->delete(
    parseId: 'parse_01G34H8J2K',
);
echo "Deleted\n";
```

```csharp C#
using Retab;
using RetabClient = Retab.Retab;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;
var client = new RetabClient(apiKey);

await client.Parses.DeleteAsync("parse_01G34H8J2K");
Console.WriteLine("Deleted");
```

```java Java
import com.retab.RetabClient;

public final class Example {
  public static void main(String[] args) throws Exception {
    RetabClient client = new RetabClient(System.getenv("RETAB_API_KEY"));

    var result = client.parses().delete("parse_abc123");
    System.out.println(result);
  }
}
```

```curl cURL
curl -X DELETE \
  'https://api.retab.com/v1/parses/parse_01G34H8J2K' \
  -H "Authorization: Bearer $RETAB_API_KEY"
```

</RequestExample>

<ResponseExample>

```text 204
No Content
```

```json 404
{
  "detail": "Parse not found"
}
```

</ResponseExample>
