---
title: 'Get'
openapi: 'GET /v1/projects/{project_id}'
---

<RequestExample>
```python python
from retab import Retab

client = Retab()
response = client.projects.get("<project_id>")
```

```javascript javascript
import { Retab } from '@retab/node';
import { config } from 'dotenv';

config();

const client = new Retab();

const response = await client.projects.get("<project_id>");
```

```bash curl
curl --request GET \
  -H "Api-Key: $RETAB_API_KEY" \
  --url https://api.retab.com/v1/projects/{project_id}
```

</RequestExample>
