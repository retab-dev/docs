---
title: 'Delete'
openapi: 'DELETE /v1/projects/{project_id}/documents/{document_id}'
---

<RequestExample>
```python python
from retab import Retab

client = Retab()
client.projects.documents.delete("<project_id>", "<document_id>")
```

```javascript javascript
import { Retab } from '@retab/node';
import { config } from 'dotenv';

config();

const client = new Retab();

await client.projects.documents.delete('<project_id>', '<document_id>');
```

```bash curl
curl --request DELETE \
  -H "API-Key: $RETAB_API_KEY" \
  --url https://api.retab.com/v1/projects/{project_id}/documents/{document_id}
```

</RequestExample>
