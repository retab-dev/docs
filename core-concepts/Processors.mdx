Processors are reusable configurations for document extraction that can be called via the API route `https://api.retab.com/v1/processors/{processor_id}/submit`.

```mermaid
graph BT
    subgraph PS["Processor Config"]
        JS["JSON Schema"]
        M["Model"]
        DPI["Image Resolution DPI"]
        RE["Reasoning Effort"]
        NC["N Consensus"]
    end
```

Processors eliminate the complexity of manually configuring extraction parameters for each document, allowing you to focus on defining your output schema and reusing proven configurations across multiple documents and automation workflows.

Use `POST /v1/processors` to create a processor and `POST /v1/processors/{processor_id}/submit` to run it.

## Processor

---

| Method       | Purpose                                                                      |
| ------------ | ---------------------------------------------------------------------------- |
| **`create`** | Creates a new processor configuration with specified extraction parameters.  |
| **`submit`** | Processes documents using an existing processor and returns structured JSON. |

### Object

<ResponseField name="Object" type="Processor">
A Processor object containing the extraction configuration.
  <Expandable title="properties">

<ResponseField name="object" type="string">
  The type of object. Always `"processor"`.
</ResponseField>

<ResponseField name="id" type="string">
  Unique identifier for the processor, prefixed with `"proc_"`.
</ResponseField>

<ResponseField name="name" type="string">
  Human-readable name of the processor.
</ResponseField>

<ResponseField name="json_schema" type="Dict[str, Any]">
  Output schema describing the JSON structure you expect to extract.
</ResponseField>

<ResponseField name="model" type="string">
  AI model used for extraction, such as `"retab-small"`.
</ResponseField>

<ResponseField name="image_resolution_dpi" type="int">
  Resolution in DPI when converting PDFs to images for vision models.
</ResponseField>

<ResponseField name="reasoning_effort" type="string">
  Reasoning effort passed to compatible models. Defaults to `"minimal"`.
</ResponseField>

<ResponseField name="n_consensus" type="int">
  Number of parallel extraction runs for consensus voting. Values > 1 enable
  consensus mode.
</ResponseField>

<ResponseField name="schema_id" type="string">
  Stable ID derived from the processor JSON schema.
</ResponseField>

<ResponseField name="schema_data_id" type="string">
  Stable data-version ID derived from the processor JSON schema.
</ResponseField>

<ResponseField name="updated_at" type="datetime">
  UTC timestamp of the last processor update.
</ResponseField>

  </Expandable>
</ResponseField>

### Create

Creates a new processor with the specified extraction configuration. The processor can then be used repeatedly to extract structured data from documents.

<ResponseField name="Returns" type="Processor">
  A newly created Processor object with the specified configuration.
</ResponseField>

<CodeGroup>

```python Request
import os
import requests

api_key = os.environ["RETAB_API_KEY"]

schema = {
    "type": "object",
    "properties": {
        "invoice_number": {"type": "string"},
        "total_amount": {"type": "number"},
    },
}

response = requests.post(
    "https://api.retab.com/v1/processors",
    headers={"Api-Key": api_key},
    json={
        "name": "Invoice Processor",
        "json_schema": schema,
        "model": "retab-small",
        "n_consensus": 3,
        "image_resolution_dpi": 150,
    },
)
response.raise_for_status()
processor = response.json()
```

```go Request
// The Go SDK does not yet model the processors API. Call /v1/processors directly.
package main

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"log"
	"net/http"
	"os"
)

func main() {
	schema := map[string]any{
		"type": "object",
		"properties": map[string]any{
			"invoice_number": map[string]any{"type": "string"},
			"total_amount":   map[string]any{"type": "number"},
		},
	}

	body, err := json.Marshal(map[string]any{
		"name":                 "Invoice Processor",
		"json_schema":          schema,
		"model":                "retab-small",
		"n_consensus":          3,
		"image_resolution_dpi": 150,
	})
	if err != nil {
		log.Fatal(err)
	}

	req, err := http.NewRequest(
		http.MethodPost,
		"https://api.retab.com/v1/processors",
		bytes.NewReader(body),
	)
	if err != nil {
		log.Fatal(err)
	}
	req.Header.Set("Api-Key", os.Getenv("RETAB_API_KEY"))
	req.Header.Set("Content-Type", "application/json")

	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		log.Fatal(err)
	}
	defer resp.Body.Close()

	payload, _ := io.ReadAll(resp.Body)
	fmt.Println(string(payload))
}
```

```ruby Ruby
# The Ruby SDK does not yet model the processors API. Call /v1/processors directly.
require 'json'
require 'net/http'
require 'uri'

schema = {
  'type' => 'object',
  'properties' => {
    'invoice_number' => { 'type' => 'string' },
    'total_amount' => { 'type' => 'number' },
  },
}

uri = URI('https://api.retab.com/v1/processors')
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

req = Net::HTTP::Post.new(uri.request_uri)
req['Api-Key'] = ENV['RETAB_API_KEY']
req['Content-Type'] = 'application/json'
req.body = JSON.generate(
  'name' => 'Invoice Processor',
  'json_schema' => schema,
  'model' => 'retab-small',
  'n_consensus' => 3,
  'image_resolution_dpi' => 150,
)

response = http.request(req)
processor = JSON.parse(response.body)
```

```php Request
<?php
// The PHP SDK does not yet model the processors API. Call /v1/processors directly.
require 'vendor/autoload.php';

use GuzzleHttp\Client as HttpClient;

$schema = [
    'type' => 'object',
    'properties' => [
        'invoice_number' => ['type' => 'string'],
        'total_amount' => ['type' => 'number'],
    ],
];

$http = new HttpClient(['base_uri' => 'https://api.retab.com/']);
$response = $http->post('v1/processors', [
    'headers' => ['Api-Key' => getenv('RETAB_API_KEY')],
    'json' => [
        'name' => 'Invoice Processor',
        'json_schema' => $schema,
        'model' => 'retab-small',
        'n_consensus' => 3,
        'image_resolution_dpi' => 150,
    ],
]);
$processor = json_decode((string) $response->getBody(), true);
```

```rust Rust
// The Rust SDK does not yet model the processors API. Call /v1/processors directly.
use serde_json::json;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let api_key = std::env::var("RETAB_API_KEY")?;

    let schema = json!({
        "type": "object",
        "properties": {
            "invoice_number": {"type": "string"},
            "total_amount": {"type": "number"}
        }
    });

    let body = json!({
        "name": "Invoice Processor",
        "json_schema": schema,
        "model": "retab-small",
        "n_consensus": 3,
        "image_resolution_dpi": 150,
    });

    let http = reqwest::Client::new();
    let response = http
        .post("https://api.retab.com/v1/processors")
        .header("Api-Key", api_key)
        .json(&body)
        .send()
        .await?
        .error_for_status()?;

    let processor: serde_json::Value = response.json().await?;
    println!("{processor}");
    Ok(())
}
```

```json Response
{
  "object": "processor",
  "id": "proc_01G34H8J2K",
  "name": "Invoice Processor",
  "json_schema": {
    "type": "object",
    "properties": {
      "invoice_number": {
        "type": "string",
        "description": "The unique invoice identifier"
      },
      "total_amount": {
        "type": "number",
        "description": "Total invoice amount including taxes"
      },
      "issue_date": {
        "type": "string",
        "description": "Invoice issue date in ISO format"
      },
      "vendor_name": {
        "type": "string",
        "description": "Name of the vendor or company"
      }
    },
    "required": ["invoice_number", "total_amount", "issue_date", "vendor_name"]
  },
  "model": "retab-small",
  "reasoning_effort": "minimal",
  "image_resolution_dpi": 150,
  "n_consensus": 3,
  "schema_id": "schema_...",
  "schema_data_id": "schema_data_...",
  "updated_at": "2025-01-21T12:34:56Z"
}
```

```csharp Request
// The .NET SDK does not yet model the processors API. Call /v1/processors directly.
using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Text;
using System.Threading.Tasks;
using Newtonsoft.Json;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;

var schema = new Dictionary<string, object>
{
    ["type"] = "object",
    ["properties"] = new Dictionary<string, object>
    {
        ["invoice_number"] = new Dictionary<string, object> { ["type"] = "string" },
        ["total_amount"] = new Dictionary<string, object> { ["type"] = "number" },
    },
};

using var http = new HttpClient();
http.DefaultRequestHeaders.Add("Api-Key", apiKey);

var payload = JsonConvert.SerializeObject(new Dictionary<string, object>
{
    ["name"] = "Invoice Processor",
    ["json_schema"] = schema,
    ["model"] = "retab-small",
    ["n_consensus"] = 3,
    ["image_resolution_dpi"] = 150,
});

var response = await http.PostAsync(
    "https://api.retab.com/v1/processors",
    new StringContent(payload, Encoding.UTF8, "application/json")
);
response.EnsureSuccessStatusCode();
var processor = JsonConvert.DeserializeObject<Dictionary<string, object>>(
    await response.Content.ReadAsStringAsync()
);
```

```typescript TypeScript
const response = await fetch("https://api.retab.com/v1/processors", {
  method: "POST",
  headers: {
    "Api-Key": process.env.RETAB_API_KEY!,
    "Content-Type": "application/json",
  },
  body: JSON.stringify({}),
});

const result = await response.json();
console.log(result);
```

</CodeGroup>

### Submit

Processes a document using an existing processor configuration and returns the extracted structured data.

<ResponseField name="Returns" type="Dict[str, Any]">
  The parsed chat completion response. The extracted data is available as the
  top-level `data` shortcut and at `choices[0].message.parsed`.
</ResponseField>

<CodeGroup>

```python Request
import os
import requests

api_key = os.environ["RETAB_API_KEY"]

with open("invoice.pdf", "rb") as invoice:
    response = requests.post(
        "https://api.retab.com/v1/processors/proc_01G34H8J2K/submit",
        headers={"Api-Key": api_key},
        files={"document": ("invoice.pdf", invoice, "application/pdf")},
    )
response.raise_for_status()
completion = response.json()
print(completion["data"])
```

```go Request
// The Go SDK does not yet model the processors API. Submit documents as multipart form data.
package main

import (
	"bytes"
	"fmt"
	"io"
	"log"
	"mime/multipart"
	"net/http"
	"os"
)

func main() {
	file, err := os.Open("invoice.pdf")
	if err != nil {
		log.Fatal(err)
	}
	defer file.Close()

	var body bytes.Buffer
	writer := multipart.NewWriter(&body)
	part, err := writer.CreateFormFile("document", "invoice.pdf")
	if err != nil {
		log.Fatal(err)
	}
	if _, err := io.Copy(part, file); err != nil {
		log.Fatal(err)
	}
	if err := writer.Close(); err != nil {
		log.Fatal(err)
	}

	req, err := http.NewRequest(
		http.MethodPost,
		"https://api.retab.com/v1/processors/proc_01G34H8J2K/submit",
		&body,
	)
	if err != nil {
		log.Fatal(err)
	}
	req.Header.Set("Api-Key", os.Getenv("RETAB_API_KEY"))
	req.Header.Set("Content-Type", writer.FormDataContentType())

	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		log.Fatal(err)
	}
	defer resp.Body.Close()

	payload, _ := io.ReadAll(resp.Body)
	fmt.Println(string(payload))
}
```

```ruby Ruby
# The Ruby SDK does not yet model the processors API. Submit documents as multipart form data.
require 'json'
require 'net/http'
require 'uri'

uri = URI('https://api.retab.com/v1/processors/proc_01G34H8J2K/submit')
http = Net::HTTP.new(uri.host, uri.port)
http.use_ssl = true

req = Net::HTTP::Post.new(uri.request_uri)
req['Api-Key'] = ENV['RETAB_API_KEY']
File.open('invoice.pdf', 'rb') do |io|
  req.set_form([['document', io, { filename: 'invoice.pdf', content_type: 'application/pdf' }]], 'multipart/form-data')
  response = http.request(req)
  completion = JSON.parse(response.body)
  puts completion['data']
end
```

```php Request
<?php
// The PHP SDK does not yet model the processors API. Submit documents as multipart form data.
require 'vendor/autoload.php';

use GuzzleHttp\Client as HttpClient;

$http = new HttpClient(['base_uri' => 'https://api.retab.com/']);
$response = $http->post('v1/processors/proc_01G34H8J2K/submit', [
    'headers' => ['Api-Key' => getenv('RETAB_API_KEY')],
    'multipart' => [
        [
            'name' => 'document',
            'contents' => fopen('invoice.pdf', 'rb'),
            'filename' => 'invoice.pdf',
            'headers' => ['Content-Type' => 'application/pdf'],
        ],
    ],
]);
$completion = json_decode((string) $response->getBody(), true);
print_r($completion['data']);
```

```rust Rust
// The Rust SDK does not yet model the processors API. Submit documents as multipart form data.
use reqwest::multipart::{Form, Part};
use std::fs;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let api_key = std::env::var("RETAB_API_KEY")?;

    let bytes = fs::read("invoice.pdf")?;
    let part = Part::bytes(bytes)
        .file_name("invoice.pdf")
        .mime_str("application/pdf")?;
    let form = Form::new().part("document", part);

    let http = reqwest::Client::new();
    let response = http
        .post("https://api.retab.com/v1/processors/proc_01G34H8J2K/submit")
        .header("Api-Key", api_key)
        .multipart(form)
        .send()
        .await?
        .error_for_status()?;

    let completion: serde_json::Value = response.json().await?;
    println!("{:?}", completion.get("data"));
    Ok(())
}
```

```json Response
{
  "id": "chatcmpl_...",
  "object": "chat.completion",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "{\"invoice_number\":\"INV-2024-0789\",\"total_amount\":1576.75}",
        "parsed": {
          "invoice_number": "INV-2024-0789",
          "total_amount": 1576.75
        }
      },
      "finish_reason": "stop"
    }
  ],
  "data": {
    "invoice_number": "INV-2024-0789",
    "total_amount": 1576.75
  }
}
```

```csharp Request
// The .NET SDK does not yet model the processors API. Submit documents as multipart form data.
using System;
using System.IO;
using System.Net.Http;
using System.Threading.Tasks;
using Newtonsoft.Json;

var apiKey = Environment.GetEnvironmentVariable("RETAB_API_KEY")!;

using var http = new HttpClient();
http.DefaultRequestHeaders.Add("Api-Key", apiKey);

using var form = new MultipartFormDataContent();
var bytes = await File.ReadAllBytesAsync("invoice.pdf");
var fileContent = new ByteArrayContent(bytes);
fileContent.Headers.ContentType = new System.Net.Http.Headers.MediaTypeHeaderValue("application/pdf");
form.Add(fileContent, "document", "invoice.pdf");

var response = await http.PostAsync(
    "https://api.retab.com/v1/processors/proc_01G34H8J2K/submit",
    form
);
response.EnsureSuccessStatusCode();
var completion = JsonConvert.DeserializeObject<Dictionary<string, object>>(
    await response.Content.ReadAsStringAsync()
)!;
Console.WriteLine(completion["data"]);
```

```typescript TypeScript
const response = await fetch("https://api.retab.com/v1/processors/proc_01G34H8J2K/submit", {
  method: "POST",
  headers: {
    "Api-Key": process.env.RETAB_API_KEY!,
    "Content-Type": "application/json",
  },
  body: JSON.stringify({}),
});

const result = await response.json();
console.log(result);
```

</CodeGroup>
